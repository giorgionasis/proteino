import type { Metadata } from "next";

export const metadata: Metadata = { title: "Νέα πρόταση — Proteino" };

export default function SubmitPage() {
  return (
    <main className="min-h-screen bg-white">
      {/* SubmissionFlow component — handles all 6 states */}
    </main>
  );
}
